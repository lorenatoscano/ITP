#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void move(int n)
{
	// int inicial[n];
	// int i;
	// for (i = 0; i < n ; ++i)
	// {
	// 	inicial[i] = n - i;
	// }

	//Caso base: quando todos estiverem empilhados na torre C
	//Processamento:
	//Chamada recursiva:
}

int main()
{
	int n;

	scanf("%d", &n);

	move(n)

	return 0;
}